<Window x:Class="MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ReviewPlanner.UI"
        mc:Ignorable="d"
        Title="ReviewPlanner" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="90"/>
            <ColumnDefinition/>
            <ColumnDefinition Width="90"/>
        </Grid.ColumnDefinitions>
        <Button x:Name="Btn_prev" Content="&lt;" HorizontalAlignment="Center" Height="50" VerticalAlignment="Center" Width="{Binding Height, ElementName=Btn_prev}"/>
        <Button x:Name="Btn_next" Content="&gt;" HorizontalAlignment="Center" Height="50"  VerticalAlignment="Center" Width="{Binding Height, ElementName=Btn_prev}" Grid.Column="2"/>
    </Grid>
</Window>
